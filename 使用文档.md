# OPC DA → OPC UA 网关 使用文档（用户手册）

> **适用对象**：现场实施工程师、运维人员  
> **软件版本**：V2.0.0（关于对话框中显示的版本号为权威版本；本文以 V2.0.0 为基线）  
> **文档性质**：操作手册，非开发指南。架构与代码细节见 `OPC_DA转UA网关开发指南.md`。

---

## 1. 功能概述

本软件是一款运行于工业现场上位机的**协议转换网关**：

- **输入**：本地/远程 OPC DA 服务器（基于 COM 的传统工业协议）。
- **输出**：内嵌 OPC UA 服务器（基于 TCP 的现代工业协议），地址 `opc.tcp://<监听地址>:<端口>`。
- **用途**：将第三方 OPC UA 客户端（SCADA、MES、历史库等）无法直接访问的 OPC DA 数据源，转换为其可拉取的 UA 节点。
- **运行目标**：单机 Windows 环境，7×24 长期稳定运行（≥30 天不中断）。
- **关键特性**：看门狗进程守护（崩溃自动重启）、30 天健康快照与内存增长告警、单实例保护、日志按日归档。

---

## 2. 运行环境要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 7 / 10 / 11 或 Windows Server |
| 运行时 | .NET Framework 4.7.2 Runtime（Win10/11 通常自带） |
| 位宽 | 必须 **x86（32 位）** 运行，因 OPC DA COM 组件为 32 位 |
| OPC DA 服务器 | 32 位 COM 进程外服务器，且其 COM 组件已正确注册 |
| 第三方采集端 | 通过 OPC UA 客户端连接，并发会话数 ≤ 50（默认配置） |

> ⚠ **位宽提醒**：若以 64 位方式启动，会因 COM 位宽不匹配导致 OPC DA 连接失败。主程序为 x86 编译产物，请勿在 64 位宿主中托管。

---

## 3. 部署

### 3.1 文件清单

部署目录需包含以下文件（Costura.Fody 已将第三方 DLL 嵌入主 exe，无需附带散落 DLL）：

| 文件 | 说明 | 是否必须 |
|---|---|---|
| `OpcDaToUaGateway.exe` | 主程序（已嵌入所有托管依赖） | ✅ |
| `OpcDaToUaGateway.exe.config` | .NET 运行时声明，**缺失会导致启动失败** | ✅ |
| `OpcDaToUaGateway.Watchdog.exe` | 看门狗进程（崩溃自动重启主程序） | 启用守护时必需 |
| `OpcDaToUaGateway.Keygen.exe` | 授权码计算工具（管理员使用，现场可不部署） | 可选 |
| `config.json` | 运行时配置文件（首次启动自动生成默认配置） | ✅ |
| `app.ico` | 应用图标 | 可选 |

> 运行时自动生成的目录（无需手动创建）：`logs/`（日志）、`Certificates/`（UA 证书）、`health/`（健康快照数据）、`tags.json`（点表数据，自 v1.3.x 起从 config.json 分离）。

### 3.2 安装步骤

1. 将以上文件复制至目标主机某一固定目录（建议路径不含中文与空格，如 `C:\Gateway\OpcDaToUa\`）。
2. 确认目标主机已安装 **.NET Framework 4.7.2 Runtime**。
3. 确认 OPC DA 服务器已在本地或远程注册可用，且 COM 组件正常。
4. 双击 `OpcDaToUaGateway.exe` 启动；首次启动会生成默认 `config.json`。

> 若需开机自启并静默驻留托盘，见第 8 节。

---

## 4. 配置（config.json）

配置文件位于程序目录下，支持**配置热加载**（通过 `FileSystemWatcher` 监听文件变化并自动重新加载，无需重启主程序即可生效——但 DA/UA 连接参数变更仍需停止并重新启动网关才能应用）。

### 4.1 完整字段说明

```json
{
  "OpcDa": {
    "ServerProgId": "Matrikon.OPC.Simulation.1",   // OPC DA 服务器 ProgID
    "UpdateRateMs": 1000,                          // DA 数据订阅刷新率（毫秒）
    "Mode": "Async",                               // 数据获取方式：Async(异步订阅) / Sync(同步轮询)
    "Tags": [                                       // 需映射的标签列表
      { "ItemId": "Random.Int32", "DisplayName": "随机整数", "DataType": "Int32" }
    ]
  },
  "OpcUa": {
    "ServerName": "OpcDaToUaGateway",              // UA 服务器名称
    "Port": 4840,                                  // UA TCP 监听端口
    "NamespaceUri": "http://gateway.example.com/OpcDaBridge/",
    "ListenAddress": "localhost",                  // localhost=仅本机；0.0.0.0=允许远程
    "SecurityMode": "None",                        // None / Sign / SignAndEncrypt
    "SecurityPolicy": "None",                      // None / Basic256Sha256 / Basic128Rsa15 / Basic256
    "AutoAcceptCertificates": false,               // 生产环境务必关闭，需手动信任客户端证书
    "MaxSessionCount": 50,                         // 最大并发 UA 会话数
    "SessionTimeout": 120000                       // 会话超时（毫秒）
  },
  "LastConnectedProgId": "",
  "AutoConnectDa": false,                          // 启动后自动连接 DA
  "AutoStartUa": false,                            // 启动后自动启动 UA 服务器
  "AutoStartWithWindows": false,                   // 开机自启（创建启动文件夹快捷方式）
  "EnableWatchdog": false,                         // 启用看门狗进程守护
  "AuthorizationCode": ""                          // 授权码（验证通过后自动保存）
}
```

### 4.2 关键配置项指引

| 配置项 | 建议 | 说明 |
|---|---|---|
| `OpcDa.ServerProgId` | 现场实际 DA 服务器 ProgID | 也可通过界面"浏览"按钮自动发现并填入；首次运行默认填充 `Matrikon.OPC.Simulation.1` |
| `OpcDa.Mode` | `Async`（默认） | `Async`=异步订阅（服务器推送，经 ValuesChanged 回调）；`Sync`=同步轮询（网关定时主动读取）。可在界面「数据获取」下拉框切换 |
| `OpcDa.UpdateRateMs` | 1000 | 刷新频率，异步模式作为订阅推送周期、同步模式作为轮询周期 |
| `OpcUa.ListenAddress` | `localhost`（默认） | 仅需本机采集时保持；远程采集客户端需改为 `0.0.0.0` 并开放防火墙端口 |
| `OpcUa.SecurityMode/Policy` | 生产用 `SignAndEncrypt` + `Basic256Sha256` | 默认 `None` 为调试便利；生产环境应启用加密并关闭 `AutoAcceptCertificates` |
| `AutoAcceptCertificates` | **生产环境 `false`** | 设为 `true` 仅在调试阶段临时使用，自动信任所有客户端证书存在安全风险 |
| `EnableWatchdog` | 长期运行建议 `true` | 主程序崩溃后由看门狗自动拉起 |

### 4.3 标签（Tags）配置

| 字段 | 说明 |
|---|---|
| `ItemId` | OPC DA 标签的完全限定名（如 `Random.Int32`、`Bucket_Brigade.Int1`） |
| `DisplayName` | 在 UA 地址空间中显示的名称 |
| `DataType` | 数据类型：`Boolean` / `Int16` / `Int32` / `UInt16` / `UInt32` / `Float` / `Double` / `String` / `DateTime` |

> 支持通过界面"获取点表"对话框浏览 DA 服务器地址空间并批量勾选，也可通过 CSV 导入/导出（见第 6 节）。`TagKey` 为内部标识，**不持久化**，每次加载重新分配，运维无需关注。

---

## 5. 主界面与日常操作

### 5.1 界面分区

| 区域 | 主要内容 |
|---|---|
| OPC DA 服务器 | ProgID 输入框、浏览按钮、获取点位按钮、数据获取方式下拉（异步订阅/同步轮询） |
| OPC UA 服务器设置 | 监听地址、端口号、安全模式、证书策略、连接数、端点 URL 预览 |
| 控制面板 | 启动/停止按钮、导出点表、自动选项（DA/UA/开机/守护）、状态标签、关于按钮 |
| 标签数据监控 | DataGridView 虚拟模式实时显示标签值、质量、时间戳（支持 5 万+ 行） |
| 运行日志 | 文本框（由 LogManager 管理，同时写入文件） |

### 5.2 标准启动流程

1. 在"OPC DA 服务器"区填入或浏览选择目标 DA 服务器 ProgID。**未选择服务器时，「获取点位」和「启动网关」按钮不可操作**。
2. 点击"获取点位"，在弹出的对话框中浏览并勾选需要映射的标签，确认后写入配置。
3. （可选）在「数据获取」下拉框选择数据获取方式：
   - **异步订阅**（默认）：依赖 OPC DA 服务器推送数据变化，经 `ValuesChanged` 回调触发。适合大多数场景。
   - **同步轮询**：网关按刷新频率定时主动读取，不依赖服务器回调。适合回调不稳定的环境。
4. （可选）在"OPC UA 服务器设置"区调整端口、安全模式等。
5. 点击"启动"按钮。启动顺序为：
   - ① 启动内嵌 OPC UA 服务器；
   - ② 连接 OPC DA 服务器并订阅标签；
   - ③ 创建 UA 节点并将 DA 数据桥接至 UA。
   - 启动过程中，日志框会实时显示节点创建进度（如"已创建 UA 变量节点: 35000/35000 (100%)"）。
6. 启动成功后，状态标签显示"已运行"，数据监控区开始刷新，日志区输出连接与节点注册信息。

> 启动后"OPC UA 服务器设置"区控件会被禁用，防止运行时误改导致不一致。

### 5.3 停止流程

- 点击"停止"按钮：网关顺序停止（先取消 DA 事件订阅 → 停止 DA 客户端 → 停止 UA 服务器）。
- 点击窗口关闭（×）：默认**最小化到系统托盘，不退出进程**。需通过托盘菜单"退出"才能真正关闭。

### 5.4 状态判断

- **状态标签颜色**：绿色=正常/已授权；橙红=未授权（试用中）或异常。
- **质量列**：`Good` 显示绿色；其他（如 `Bad`）显示红色，表示数据源质量异常（断连或坏点）。
- **内存告警**：状态栏在内存增长达到阈值时变黄（7/30 天工作集增长 ≥20%）或变红（≥50%），详见第 9 节。

---

## 6. 点表导入/导出

- **主程序"导出点表"**：从当前已配置标签列表导出为 CSV（含服务器、时间戳、点位数、刷新频率头信息）。
- **"获取点位"对话框内导入/导出**：独立 CSV 导入导出按钮。
  - 导出格式含 `序号,ItemId,名称,数据类型,描述`。
  - 导入：跳过 `#` 注释行与表头；支持 RFC 4180 引号格式；第 2 列（或唯一列）作为 ItemId；与已浏览列表匹配并勾选；若浏览列表为空（离线），则直接从 CSV 创建点位。

---

## 7. 授权管理

### 7.1 试用与授权

- 未授权时软件可**试用 30 分钟**，到期后自动关闭且无法再启动。
- 授权状态在主界面状态标签显示："已授权"（绿）/"未授权（试用中）"（橙红）。

### 7.2 获取授权码流程

1. 点击"关于"按钮打开关于对话框。
2. 复制本机 **PCID**（机器码，由 CPU、主板、BIOS 硬件信息经 SHA256 生成，16 位十六进制）。
3. 将 PCID 发给管理员，由 `Keygen.exe` 生成授权码（格式 `XXXX-XXXX-XXXX-XXXX-XXXX`）。
4. 在关于对话框输入授权码，点击验证；验证通过后授权码自动保存到 `config.json`，状态转为"已授权"。

> Keygen 用法：交互模式直接运行按菜单操作；命令行模式 `Keygen.exe <PCID>` 直接输出授权码，适合脚本调用。

### 7.3 注意事项

- 授权码与特定硬件（PCID）绑定，**不可复制到其他机器使用**。
- 若更换主板/CPU 等硬件导致 PCID 变化，需重新申请授权码。
- 若"关于"对话框显示授权异常，见《故障恢复预案》"授权失效"场景。

---

## 8. 系统托盘、开机启动与看门狗

### 8.1 系统托盘

- 关闭窗口按钮 = 最小化到托盘（非退出）。
- 双击托盘图标 = 显示主窗口。
- 托盘右键菜单：显示主窗口 / 退出。

### 8.2 开机启动

- 勾选"自动选项"中的"开机启动"，程序通过 Windows 启动文件夹 `.lnk` 快捷方式（参数 `--minimized`）实现开机静默驻留托盘。
- 命令行参数 `--minimized`：启动时直接最小化到系统托盘。

### 8.3 看门狗（进程守护）

- 勾选"进程守护"启用看门狗独立进程。主程序崩溃/无响应时，看门狗在数秒内自动重启主程序。
- 心跳机制：主程序定时发送心跳事件；看门狗超时未收到判定为无响应并重启。
- **退出语义**：
  - 取消勾选"进程守护" → 看门狗收到停止信号后退出。
  - 托盘右键"退出" → 主程序正常退出，看门狗继续运行但**不重启**主程序（待下次需要时手动启动）。
  - 主程序崩溃 → 看门狗自动重启。
- 看门狗快速重启有保护：60 秒内最多重启 3 次，防止崩溃循环导致主机负载飙升。

---

## 9. 日志与诊断

所有日志位于程序目录下：

| 文件/目录 | 说明 |
|---|---|
| `logs/gateway_yyyy-MM-dd.log` | 主程序运行日志，按日分文件，保留 30 天 |
| `logs/watchdog.log` | 看门狗日志（单文件，保留 7 天） |
| `crash.log` | 未处理异常记录（程序目录根，非 logs 下），崩溃排查首要文件 |
| `dropped_warnings.log` | 日志队列满时旁路写入的丢弃告警（异常高负载时） |
| `health/health_daily.jsonl` | 每日健康聚合数据（工作集/CPU/标签数/增长率等），90 天轮转 |
| `health/health_*.json` | 健康快照明细 |
| `Certificates/` | UA 证书目录（Own/Trusted/Rejected/Issuers） |

**排查建议**：
- 日常异常先查 `logs/gateway_yyyy-MM-dd.log`。
- 程序无响应/崩溃先查 `crash.log`。
- 看门狗行为异常查 `logs/watchdog.log`。
- 内存增长告警依据 `health/health_daily.jsonl`，阈值：7/30 天工作集增长 ≥20% 黄色、≥50% 红色。

---

## 10. 常见问题（FAQ）

| 现象 | 可能原因 | 初步处置 |
|---|---|---|
| 双击 exe 无反应或立即退出 | 缺少 `.exe.config` 或 .NET 4.7.2 未安装 | 确认 `OpcDaToUaGateway.exe.config` 存在；安装 .NET Framework 4.7.2 Runtime |
| 提示"已在运行中" | 已有实例（含托盘中） | 检查系统托盘；或从任务管理器结束残留进程后重试 |
| DA 连接失败 | ProgID 错误 / COM 未注册 / 位宽不匹配 | 用"浏览"重新发现 DA 服务器；确认 DA 服务器为本机已注册 32 位 COM；确认程序以 x86 运行 |
| UA 客户端连不上 | 监听地址为 localhost / 端口被占 / 防火墙 | 远程采集需将 `ListenAddress` 改为 `0.0.0.0` 并开放端口；检查 4840 端口占用 |
| UA 客户端证书被拒 | `AutoAcceptCertificates=false` 且未手动信任 | 将客户端证书放入 `Certificates/Trusted/` 或临时开启自动接受（调试） |
| 数据长时间不变 | DA 异步回调丢包 | 程序每 5 分钟有定时同步兜底读取；若仍停滞检查 DA 服务器质量 |
| 试用到期无法启动 | 未输入有效授权码 | 见第 7 节重新申请授权码 |
| 内存告警变红 | 长期运行内存增长超 50% | 见《故障恢复预案》"内存增长告警"场景 |
| 获取点位/启动网关按钮不可用 | 未选择 OPC DA 服务器 | 先点击"浏览..."选择服务器，按钮将自动启用 |

---

*本使用文档基于 V2.0.0 源码与项目开发指南整理，供现场实施与运维参考。如遇文档与软件实际行为不符，以软件界面与日志为准，并反馈更新。*
