# OpcDaToUaGateway 本地编译步骤（V2.4.0）

> 本文件用于在本机（Windows + .NET SDK + Windows 桌面工作负载）重新编译。
> 沙箱环境无任何 .NET 构建工具链（无 `dotnet`、无 `MSBuild`），无法代编；以下为手动步骤。

## 1. 前提条件
- 操作系统：**Windows**（本工程为 WinForms 桌面程序，`PlatformTarget=x86`，需 Windows 运行/构建）。
- 工程本质：**SDK 风格 `net472` 工程**（`<Project Sdk="Microsoft.NET.Sdk">`，`TargetFramework=net472`，`UseWindowsForms=true`）。
  - ⚠️ 因此**必须使用 `.NET SDK` / `dotnet` CLI 构建**，而非旧式 `dotnet Framework` 命令行。
  - 主工程 csproj 内含 `BuildAndCopyWatchdog` / `BuildAndCopyKeygen` 目标，会通过 `dotnet build` 编译并复制 Watchdog、Keygen 子项目，故 `dotnet` 不可或缺。
- 所需组件（**任一**即可，均需在 Windows 上提供 net472 目标包 + WinForms 引用程序集）：
  - **Visual Studio 2019 / 2022**，安装时勾选「**.NET 桌面开发**」工作负载（自动含 .NET SDK、net472 目标包、WinForms）。
  - 或 **Visual Studio Build Tools** + 「.NET 桌面生成工具」工作负载。
  - 单独安装 **.NET SDK**（如 8.0/9.0/10.0）后，还需补装 **.NET Framework 4.7.2 目标包 / 开发人员包**，否则 `dotnet build -f net472` 会找不到 WinForms 引用程序集。

## 2. 编译方法
### 方式 A：dotnet CLI（推荐，最贴近工程目标）
在项目根目录执行：
```bat
dotnet build OpcDaToUaGateway.sln -c Release -v minimal
```
- 该命令会自动还原 NuGet 包，并按 csproj 中的目标依次构建主程序、Watchdog、Keygen，最后将子程序 EXE 复制到主程序输出目录。
- 如需调试配置，将 `Release` 改为 `Debug`。

### 方式 B：Visual Studio
1. 打开 `OpcDaToUaGateway.sln`
2. 顶部工具栏选择配置：`Release`（推荐）或 `Debug`
3. 菜单「生成 → 生成解决方案」（快捷键 `F6`）

### 方式 C：MSBuild 命令行（VS 开发人员命令提示符）
```bat
:: 需先运行 VS 的 "Developer Command Prompt"，确保 MSBuild 与 net472 目标包可用
msbuild OpcDaToUaGateway.sln /p:Configuration=Release /t:Rebuild
```
> 注意：MSBuild 需安装完整的 Visual Studio 或 Build Tools 工作负载。若系统中无 `msbuild.exe`（不在 PATH 中），请使用方式 A 或 B。

## 3. 产物位置
| 项目 | 输出路径 |
|---|---|
| 主程序 | `bin/Release/net472/OpcDaToUaGateway.exe` |
| Keygen 工具 | `Keygen/bin/Release/net472/OpcDaToUaGateway.Keygen.exe` |
| Watchdog | `Watchdog/bin/Release/net472/OpcDaToUaGateway.Watchdog.exe` |
> 注：SDK 风格输出路径含 `net472` 子目录（旧式输出为 `bin/Release/`），以实际 `dotnet build` 生成为准。

## 4. 验证标准
- 编译结果：**0 警告 0 错误**（与 handoff 基线一致）。
- 版本一致性（当前统一为 V2.4.0）：
  - 关于对话框「版本」显示 `2.4.0`
  - 主窗口标题为 `OPC DA → OPC UA 网关`（不含版本号）
  - Keygen 工具版本号 `2.4.0`
  - Watchdog 版本号 `2.4.0`
- 运行：先启动主程序 `OpcDaToUaGateway.exe`；Watchdog 负责守护主进程（按需配置开机自启）。

## 5. 常见报错与处理
- **找不到 net472 / WinForms 引用程序集**：未安装「.NET 桌面开发」工作负载或 .NET Framework 4.7.2 目标包。装 VS 桌面工作负载或目标包后重试。
- **NuGet 还原失败（离线）**：先在有网环境 `dotnet restore`，或将 `packages` 缓存拷贝至 `%USERPROFILE%\.nuget\packages`。
- **x86 平台 COM 调用异常**：本工程 `PlatformTarget=x86`（OPC DA 多为 32 位 COM），须以 x86 运行，勿改为 AnyCPU。
- **MSBuild 未找到**：`msbuild` 命令不在 PATH 中。这是正常的——本机安装了 .NET SDK 但未安装完整 Visual Studio。使用 `dotnet build` 即可，无需 MSBuild。

## 6. 说明
- 本工程为 git 仓库，源码与构建产物分离（`bin/`、`obj/` 在 `.gitignore` 中）。
- 三个子项目（主程序、Keygen、Watchdog）的版本号已统一为 V2.4.0。
- 本工程当前已是 git 仓库，可通过 GitHub Actions 云端编译（推送至 `main`/`master` 后自动构建）。
